version: '3.7'

services:
  rabbitmq:
    ports:
      - 5672:5672

  registry:
    build: registry

  jwt-service:
    build: jwt-service
    ports:
      - 9000:9000

  geo-api:
    build: geo-api
    ports:
      - 8088:8088

  geo-wrapper:
    build: geo-wrapper

  gateway:
    build: gateway

#  portal-ui:
#    build: portal-ui
