meta {
  name: create new template
  type: http
  seq: 2
}

post {
  url: http://10.10.10.131:8090/templates
  body: multipartForm
  auth: bearer
}

headers {
  Content-Type: multipart/form-data
}

auth:bearer {
  token: ey
}

body:multipart-form {
  dto: {"name":"feature_extract_full_flat","title":"feature_extract_full_flat"} @contentType(application/json)
  file: @file(/path/file.docx) @contentType(multipart/form-data)
}
