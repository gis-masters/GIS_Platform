import {APP_VERSION} from './var';

export const environment = {
  production: false,
  version: APP_VERSION,
  server: {
    'host': 'localhost',
    'port': 8100
  },
  scratchWorkspaceName: 'scratch_workspace'
};
