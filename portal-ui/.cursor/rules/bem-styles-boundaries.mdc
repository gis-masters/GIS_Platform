---
description: Стили сущности совпадают по границам с её TS/JSX-файлом; чужие блоки и элементы не описывать
globs: "**/components/**/*.{tsx,scss}"
alwaysApply: false
---

# Стили: одна сущность — один файл

Стили одной сущности не содержат описание другой. В CSS блока не может быть описания другого блока.

Границы стилей совпадают с границами TS/JSX:

- блок и его элементы в **одном** `.tsx` → стили в **одном** `.scss` рядом;
- элемент вынесен в отдельный файл → и его стили в отдельном `Block-Element.scss` рядом с ним;
- в `Block.scss` — только корень блока (нет корневых стилей — файл не заводить).

```
# элементы вынесены
Block/
  Block.tsx / Block.scss
  Element/Block-Element.tsx / Block-Element.scss

# всё в одном файле
Block/
  Block.tsx   # блок + элементы
  Block.scss  # .Block и &-Element вместе
```

```scss
/* ❌ в стилях BlockA описывать BlockB или чужой элемент */
.BlockA {
  .BlockB { ... }
}
```
