<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="STASH" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/opt/logs/wrapper.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/opt/logs/wrapper.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } [%t] --- %-40.40logger{39} : %m%n</pattern>
        </encoder>
    </appender>

    <logger name="ru.mycrg.wrapper" level="DEBUG"></logger>

    <root level="INFO">
        <appender-ref ref="STASH"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
