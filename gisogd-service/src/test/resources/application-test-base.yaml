spring:
  security:
    oauth2:
      client:
        registration:
          dataServiceClient:
            client-id: test-client-id
            client-secret: test-client-secret
          gisogdRfClient:
            client-id: test-gisogd-client-id
            client-secret: test-gisogd-client-secret
  rabbitmq:
    host: localhost
    username: test
    password: test

camel:
  springboot:
    auto-startup: false